spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/Desktop/food-delivery/config-repo
          default-label: main
          force-pull: true
          clone-on-start: true


server:
  port: 8888