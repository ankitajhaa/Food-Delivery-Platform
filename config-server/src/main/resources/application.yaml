spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: file:///config-repo
        native:
          search-locations: file:///config-repo

server:
  port: 8888